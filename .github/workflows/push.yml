on:
  push

name: ci

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: docker-practice/actions-setup-docker@master
      - run: |
            set -x

            docker version

            ls -lah
            cd development
            docker-compose up -d
            
      
